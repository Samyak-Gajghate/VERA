# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtual environments
venv/
.venv/
env/

# Test artifacts
.pytest_cache/
*.egg-info/
dist/
build/
.coverage
htmlcov/

# Development env files (never bake secrets into image)
.env
.env.local
.env*.local

# Alembic offline migration artifacts (run separately, not in container)
alembic/

# Dev-only scripts and test runner files
test_graph.py
tests/

# Local docs used only for ingestion, not needed at runtime
# (ingestion is run via /upload endpoint or separate ingest_docs.py run)
